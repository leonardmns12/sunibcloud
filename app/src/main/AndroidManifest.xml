<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.leydevelopment.sunibcloud">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="org.apache.http.legacy"
        android:required="false" />

    <application
        android:name=".utils.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".ui.PreviewImage"></activity>
        <activity android:name=".ui.Maintenance" />
        <activity android:name=".ui.PersonalChanges" />
        <activity android:name=".ui.NoNetwork" />
        <activity android:name=".ui.ViewInformation" />
        <activity android:name=".ui.Information" />
        <activity android:name=".ui.SuccessLogin" />
        <activity android:name=".ui.ConnectAccount" />
        <activity android:name=".ui.TrashbinActivity" />
        <activity android:name=".ui.UserActivity" />
        <activity android:name=".ui.OtpActivity" />
        <activity android:name=".ui.Authentication" />
        <activity android:name=".ui.Register" />
        <activity android:name=".ui.Login" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>